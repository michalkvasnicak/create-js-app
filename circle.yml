machine:
  node:
    version: 6.9.1

dependencies:
  cache_directories:
    - node_modules
    - ~/.yarn-cache
  override:
    - curl -o- -L https://yarnpkg.com/install.sh | bash

test:
  override:
    - ./tasks/test.sh
